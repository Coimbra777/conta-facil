# Arquitetura (PT-BR)

A documentação técnica mantida em paralelo está em **[ARCHITECTURE.md](./ARCHITECTURE.md)** (camadas, Laravel 12, React/Vite, MySQL, modelo `User → Expense → ExpenseParticipant → Charge → PaymentProof`, decisões e diagramas).

Use esse arquivo apenas se um link antigo apontar para `doc/arquitetura.md`.
