# URL base do Laravel (sem barra final). Obrigatória para login/cadastro real e chamadas à API.
VITE_API_BASE_URL=http://localhost:8000

# O modo demonstração (botão na tela de login) usa apenas dados locais e não depende desta URL.
