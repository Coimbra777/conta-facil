# Opcional. Se omitir: em dev usa http://localhost:8000; em build de produção as chamadas usam
# a mesma origem do SPA (ex.: Laravel servindo public/spa — URLs relativas /api/v1).
# Defina quando a API estiver em outro domínio (CORS).
# VITE_API_BASE_URL=https://api.exemplo.com

# O modo demonstração (botão na tela de login) não depende desta variável.
